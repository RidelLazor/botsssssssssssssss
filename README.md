# RidelLazor Announcements Bot

## Setup

1. Rename `.env.example` to `.env`
2. Fill in your TOKEN, CLIENT_ID, and GUILD_ID
3. Run:

npm install
npm run deploy
npm start

## Command

/announce title:"Your Title" message:"Your Message" ping:true
